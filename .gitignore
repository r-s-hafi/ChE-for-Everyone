# The site has no build output and no dependencies, so this covers OS clutter
# and the Python artifacts from simulator/.
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
