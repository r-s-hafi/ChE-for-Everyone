# content/literature/ — reserved

Recommended books, papers, and standards, with notes on what each one is actually good for.

**Nothing here yet, and `/literature` is not a route.** This is a visible slot, per [docs/pds.md](../../docs/pds.md) sections 3 and 8.

Note that the site has no generator, so if this section gets built its pages will be hand-written HTML alongside the rest, not Markdown in this folder. The directory stands as the placeholder the spec asked for.
